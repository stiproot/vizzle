{
  "name": "vizzle-web",
  "private": true,
  "description": "JS dependencies for vizzle's interactive HTML output (managed with bun)",
  "type": "module",
  "scripts": {
    "sync-assets": "bun run sync-assets.ts",
    "inspect-component": "bun run inspect-component.mjs"
  },
  "dependencies": {
    "d3": "^7.9.0"
  },
  "devDependencies": {
    "puppeteer-core": "^25.10.0"
  }
}
